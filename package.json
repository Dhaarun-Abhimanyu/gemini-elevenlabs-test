{
  "name": "history-revealed",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend/chat && npm start\""
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}